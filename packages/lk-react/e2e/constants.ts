/**
 * Must mirror `apps/lk-example-vite/src/config.ts`. Duplicated rather than
 * imported to avoid a cross-package, cross-rootDir import in the e2e suite.
 */
export const LRS_ENDPOINT = 'https://mock-lrs.learning-kit.test/xapi/statements';
