# Internationalisation

*Applies to `@intellectif/lk-react` 7.1.0 and later.*

The SDK renders two kinds of text, and they are not the same problem:

| | Whose words | Where the language comes from |
|---|---|---|
| **Authored content** — questions, options, passages, hints, feedback, rubric criteria | The author's | The content itself. `stimulus.locale` puts a `lang` on that passage; `locale` on activity **data** labels xAPI statements, and on a dictation it is also the language and direction of the dictation's own words ([below](#a-dictations-own-words)) |
| **Chrome** — "Submit", "Question 3 of 10", "No plays remaining", the marking glyphs' announcements | The SDK's | `<LkIntlProvider>`, or a `strings` prop |

Before 7.1.0 the chrome was English literals, so a learner sitting a Spanish
paper read a Spanish passage inside an English scaffold, and a listening exam
could not say "No quedan reproducciones" at all. `<LkIntlProvider>` replaces
every one of those strings.

**The SDK ships the mechanism and English — no other bundled locale.** Shipping
Spanish, Portuguese or Arabic would mean shipping translations nobody in this
repository can review, and on a summative paper a wrong "No plays remaining" is
worse than an English one a teacher can see is untranslated. The words belong to
whoever speaks the language.

## Quick start

```tsx
import { LkIntlProvider } from '@intellectif/lk-react/i18n/LkIntlProvider';

<LkIntlProvider
  locale="es"
  strings={{
    submit: 'Enviar',
    checkAnswers: 'Comprobar respuestas',
    previous: 'Anterior',
    next: 'Siguiente',
    questionProgress: (index, total) => `Pregunta ${index} de ${total}`,
    wordCount: (n) => `${n} ${n === 1 ? 'palabra' : 'palabras'}`,
    media: { noPlaysRemaining: 'No quedan reproducciones' },
  }}
>
  <ActivitySequence activities={paper} />
</LkIntlProvider>;
```

Anything you omit keeps its English default, so a partial translation degrades
to mixed rather than to blank.

## Precedence

Later wins, key by key — a `media` or `stimulusKind` object is merged one level
deep rather than replaced, so overriding one label never blanks its siblings.

1. `DEFAULT_STRINGS` (English)
2. `<LkIntlProvider strings={…}>`
3. The component's own `strings` prop — `<MultipleChoice strings={{ submit: 'Entregar' }} />`
4. `mediaStrings` on a component, and `mediaBudget.strings` on `<ActivitySequence>` — both from 7.0.0, both still honoured, and both still the last word for the transport

A component without a provider above it renders the English defaults, so
`<MultipleChoice>` still works with no setup at all.

A second `<LkIntlProvider>` **replaces** the one above it rather than layering
onto it: it starts again from the English defaults, exactly as a nested
`<ThemeProvider>` starts again from `defaultTheme`. `locale` and `direction` do
not inherit from the outer provider either. To relabel one activity inside a
translated tree use that component's own `strings` prop, which does layer (step
3 above); to combine two provider levels deliberately, compose them yourself
with the exported `mergeStrings`:

```tsx
<LkIntlProvider strings={mergeStrings(spanish, sectionOverrides)}>…</LkIntlProvider>
```

There is a second `locale`, on the components. `<ActivitySequence locale>` and
each activity's `locale` prop put a `lang` on that component's own root, so they
should carry the same value you give the provider — the SDK's chrome renders
inside them. Authored content in a *different* language belongs on
`stimulus.locale`, which the stimulus panel puts on the passage alone — except a
dictation's, whose own `data.locale` places its words.

### A dictation's own words

`<Dictation>` reads `data.locale` as the language of the dictation. It puts `lang`
and `dir` on the content it shows — the title, the revealed hint, the marked
words, the sentence diff and the solution — and nowhere else: the answer box,
the list's name and the hidden per-word sentences a screen reader hears are
interface text, and keep the interface's language.

The direction comes from the tag when the tag names one, read from the tag
alone, so a server rendering and the browser that hydrates it agree on it: a
script subtag decides (`ku-Arab`, `pa-Arab` and `pal-Phlv` are right-to-left,
`ar-Latn` and `ku-Latn` left-to-right), unless it is a code for no particular
script (`Zyyy`, `Zxxx` and the other `Z` codes) or a private-use one
(`Qaaa`–`Qabx`); without one, a language written right to left in every script
it is commonly written in is right-to-left — `acm`, `aeb`, `aii`, `ajp`,
`apc`, `ar`, `arc`, `ars`, `ary`, `arz`, `azb`, `bqi`, `ckb`, `dv`, `fa`,
`glk`, `hbo`, `he`, `iw`, `ji`, `jrb`, `khw`, `lrc`, `mzn`, `nqo`, `ota`,
`pnb`, `prs`, `ps`, `sdh`, `skr`, `syc`, `syr`, `ur` or `yi`. A private-use or
grandfathered tag (`x-arab`, `i-klingon`) names nothing. `-` and `_` both
separate subtags, so `ar_EG` is right-to-left too, and is written into `lang`
as `ar-EG`, the only form `lang` accepts. This is not `directionForLocale()`,
which sets the interface's direction ([below](#writing-direction)) from the
language alone — to it `ku-Latn` is right-to-left and `ar_EG` left-to-right —
so give the provider the interface's locale, not the dictation's.

A tag that names no direction — `en`, or `ku`, `sd` and `ug`, which are
written in more than one direction — leaves it to the text. The title takes its
direction from its own text (`dir="auto"`), and the rest from the first letter
or directional mark of the transcript — or of the stored words, when a review
has only those — never from the list's own text, whose first strong character
could be a hidden interface sentence or whatever the learner typed. A transcript
that starts with a right-to-left mark (U+200F) or an Arabic letter mark (U+061C)
is laid out right to left, whatever letter follows; one that starts with a
left-to-right mark (U+200E), left to right. With no letter or mark anywhere, they
follow the interface's direction. Text is read by the runtime's Unicode tables,
so a server rendering and a browser agree on every character both know: a
letter added in a later Unicode version than the server's reads as no letter
there, and its direction, like the hint count of a transcript made of such
characters, can differ between the two. Without a `locale` there is no `lang`, so a
screen reader voices the words in the interface's language: give every
dictation its `locale`.

## Interpolation and plurals are functions

There is no message format, no catalogue file and no runtime dependency. A
string that needs a value is a function:

```ts
questionProgress: (index, total) => `Pregunta ${index} de ${total}`,
wordCount: (n) => `${n} ${n === 1 ? 'palabra' : 'palabras'}`,
stimulusRange: (first, last) =>
  first === last ? `Pregunta ${first}` : `Preguntas ${first}–${last}`,
```

A format string like `"Question {n} of {total}"` needs a parser, and a parser
needs plural rules for every locale the SDK does not know. A function hands both
to you, who already have `Intl.PluralRules` and know your own language. It also
lets a translation reorder its arguments, which a positional token cannot, and
TypeScript checks the arity at the call site.

## Writing direction

`<LkIntlProvider>` renders a wrapper that carries `lang` and `dir` **only when
you supplied something that determines them**. With no `locale` and no explicit
`direction`, neither attribute is written, so an RTL host page keeps its own
direction instead of being flipped back to left-to-right by a provider that was
mounted only to translate text. `direction` defaults to `auto`, which derives
the direction from `locale`: `ar`, `arc`,
`dv`, `fa`, `he`, `ku`, `ps`, `sd`, `ug`, `ur` and `yi` are right-to-left, and
everything else is left-to-right. `directionForLocale()` is exported if you need
the same answer elsewhere, and `useLkDirection()` reports the direction in
force.

Set `direction` explicitly to override what the locale implies — for a locale
the SDK's list does not know, or to keep an English scaffold around
right-to-left content:

```tsx
<LkIntlProvider locale="ar" direction="ltr">…</LkIntlProvider>
```

`useLkDirection()` always reports a concrete `ltr` or `rtl` (it answers "which
way does this read", and falls back to `ltr`); it is only the DOM attribute that
is withheld when nothing declared one.

`skin.css` uses logical properties throughout, so the optional skin follows the
direction; this is verified by measured geometry in a real browser, not by
inspection. `defaults.css` sets no directional properties at all.

`lang` is set only when you pass a `locale`. Claiming `lang="en"` for a tree
nobody labelled would make a screen reader read a Spanish passage in an English
voice, which is worse than leaving it to the host document.

## What is NOT translated

- **Thrown errors.** Every `throw` in the package is addressed to the developer
  who wired the component up, not to the learner. Translating them would make
  them unsearchable and would put the one audience that can act on them behind a
  translation. They stay English.
- **Authored content.** Questions, options, hints, feedback, rubric criteria and
  `alt` text are yours; the SDK renders what you give it.
- **Draft issue messages.** `validateDraft` in `lk-core` returns English
  messages for an author, and the SDK never renders them. Translate by `code`:
  the codes the authoring guide documents are stable, and any other code is the
  schema library's own, which needs one generic message of your own.
- **`unscorable` reasons and codes.** These are developer-facing diagnostics.
  The component surfaces the *state* (`couldNotBeGraded`), which is translated,
  and leaves the diagnostic on the element — `data-reason` on a written
  response, `data-code` on a read aloud. A read aloud never puts a code in front
  of a learner in any mode: the two that mean "nothing was heard" become
  `readAloudNotHeard` and every other one becomes `readAloudNotAssessed`.
- **Numbers and dates.** The SDK formats no dates. It renders three numbers: a
  whole percentage and a word count, both passed to a string you control, and
  the audio transport's `m:ss / m:ss` clock, which it formats itself. The clock
  is digits and punctuation — identical in English, Spanish and Portuguese — so
  it carries no key. The scrubber's *spoken* value does carry one
  (`media.timeValue`), because that one has a word in it.

## The full surface

196 keys. Every one is proven to reach the DOM by a test that overrides all of
them with sentinels and drives the real components — including every submitting
path in every render mode. Two things fail that test: a key no component reads
(by name), and an English default that survives a full override (which is what
catches a component keeping a hardcoded literal).

### Submission

| Key | English default | Where |
|---|---|---|
| `submit` | `Submit` | Multiple Choice, Written Response |
| `checkAnswers` | `Check answers` | Fill in the Blanks, Gap Select and Dictation, `practice` |
| `submitAnswers` | `Submit answers` | Fill in the Blanks, Gap Select and Dictation, `exam` |

### Outcome

| Key | English default | Where |
|---|---|---|
| `scoreAnnouncement(percent, passed)` | `Score 82%. Passed.` | Every activity that shows a score |
| `answerSubmitted` | `Answer submitted.` | Announced on submit, before the score |
| `notGradedYet` | `Not graded yet.` | A `deferred` outcome — never rendered as 0% |
| `noGradeAvailable` | `No grade available.` | An `unscorable` outcome |

`percent` arrives already rounded. Authored overall feedback is appended
separately by the component, so a translation of this sentence never has to
carry the author's words.

### Tries and what they cost

Shown in `practice` when a [scoring policy](./scoring.md) gives a question more than one try, or
charges for hints.

| Key | English default | Where |
|---|---|---|
| `tryAgain` | `Try again` | After a graded answer short of full marks, while the policy has tries left |
| `showAnswer` | `Show answer` | Beside it: ends the question's tries, and the right answer shows |
| `keepAnswer` | `Keep this answer` | The same, where the paper shows no right answer to close on |
| `triesLeft(tries, costPercent)` | `1 try left. Each costs 25% of the marks.` (`2 tries left.` when a try costs nothing) | After the score, while another try is on offer |
| `countedTry(counts, percent)` | `Your first try counts. This one scored 100%.` | After the score, when an earlier try is the one that counts; `counts` is `'first'` or `'best'` |
| `scoreBeforeCosts(percent)` | `Before hints and tries, this answer scored 100%.` | After the score, when what hints and tries cost has lowered it |
| `hintCost(percent)` | `Each hint costs 10% of this question's marks.` | Beside the hints, before one is asked for |

### Feedback disclosure

| Key | English default |
|---|---|
| `showFeedback` | `Show feedback` |
| `hideFeedback` | `Hide feedback` |

### Fill in the Blanks

| Key | English default | Where |
|---|---|---|
| `blankLabel(ordinal)` | `Fill in blank 1` | Accessible name of each input; `ordinal` is 1-based |
| `showHint` | `Show hint` | |
| `hideHint` | `Hide hint` | |

### Written Response

| Key | English default | Where |
|---|---|---|
| `responseSubmitted` | `Response submitted. It will be graded and your result will appear here later.` | Announced on hand-in. Distinct from `awaitingGrade`, which is the `review`-mode state of a submission made earlier |
| `wordCount(count)` | `12 words` | Live counter |
| `wordBounds(min, max)` | `50–200 words`, or `up to 200 words` when `min` is 0 | Authored bounds |
| `notApplicable` | `Not applicable` | A rubric criterion the grader marked N/A |
| `awaitingHumanReview` | `This grade is awaiting review by a teacher.` | `requiresHumanReview` on the grade — learner-visible by design |
| `awaitingGrade` | `Not graded yet. This response is waiting for its grade.` | Submitted, nothing back yet |
| `couldNotBeGraded` | `This response could not be graded.` | `unscorable` |

### Sequence pager

| Key | English default | Where |
|---|---|---|
| `previous` | `Previous` | |
| `next` | `Next` | |
| `questionProgress(index, total)` | `Question 3 of 10` | The progress status *and* the question region's accessible name; `index` is 1-based |
| `unsupportedActivity` | `This activity type has no renderer. Supply one through the renderers prop.` | No renderer registered for the type |

### Gap select

| Key | English default | Where |
|---|---|---|
| `gapLabel(ordinal)` | `Choose the answer for gap 2` | Accessible name of each gap's selector in `<GapSelect>`; `ordinal` is 1-based |
| `gapPlaceholder` | `Choose…` | The empty first entry of a gap's selector — how a learner leaves a gap alone, and takes an answer back |

### Dictation

| Key | English default | Where |
|---|---|---|
| `dictationInputLabel` | `Type what you hear` | Accessible name of the answer box in `<Dictation>` |
| `dictationRecording` | `Recording` | Legend of the group holding the recording |
| `dictationSlowRecording` | `Slow recording` | Legend of the group holding the slower recording, and its `alt` when the author gave none |
| `dictationRevealNextWord(revealed, total)` | `Reveal the next word (0 of 6 shown)` | The progressive-hint button, `practice` only |
| `dictationResetHints` | `Reset hints` | Hides every revealed hint word again |
| `showSolution` | `Show solution` | The solution toggle, once the attempt is marked |
| `hideSolution` | `Hide solution` | |
| `dictationSolutionLabel` | `Solution` | Accessible name of the revealed transcript |
| `dictationMarksLabel` | `Your answer, word by word` | Accessible name of the marked word list |
| `dictationDiffNote` | `Compared after ignoring case, punctuation and extra spaces.` | Under the marks: the learner sees the normalised form and is told why |
| `dictationWordCorrect(word)` | `“cat” is correct` | The hidden sentence a screen reader hears for a correct word — the marks' only assistive-technology channel; the glyphs and the character diff are decoration |
| `dictationWordWrong(word, expected)` | `“cta” should be “cat”` | |
| `dictationWordMissing(expected)` | `“cat” is missing` | |
| `dictationWordExtra(word)` | `“the” is extra` | |
| `dictationWordWrongUnnamed(word)` | `“cta” is wrong` | In place of `dictationWordWrong` where the right answer may not show: the paper withholds solutions, or another try is on offer |
| `dictationWordMissingUnnamed` | `A word is missing` | In place of `dictationWordMissing`, on the same terms |
| `dictationLegendCorrect` | `Correct` | The legend rows; the character-level marks reuse the same four |
| `dictationLegendWrong` | `Wrong` | |
| `dictationLegendMissing` | `Missing` | |
| `dictationLegendExtra` | `Extra` | |
| `dictationNothingTyped` | `Nothing to compare: no words were entered.` | Marked result of an attempt with no words in it once punctuation and spacing are ignored — an empty one, or one of only punctuation |
| `dictationWordsSummary(correct, total)` | `4 of 6 words correct.` (`1 of 1 word correct.`) | Appended to the score announcement on submit, before any authored feedback: end it as a sentence |

### Read aloud

The learner records; the SDK never judges. Every sentence here is about the
*recording* — making one, sending it, or being told why nothing came back — and
none of them names an engine, a code or a vendor.

| Key | English default | Where |
|---|---|---|
| `readAloudModelRecording` | `Model recording` | Legend of the group holding the model reading |
| `readAloudSlowRecording` | `Slow model recording` | Legend of the group holding the slower model, and its `alt` when the author gave none |
| `readAloudRecord` | `Record` | Starts a take |
| `readAloudStop` | `Stop recording` | Ends the take in progress |
| `readAloudRerecord` | `Record again` | Replaces the button above once a take exists — it throws that take away |
| `readAloudYourRecording` | `Your recording` | Accessible name of the player for the learner's own take, in every mode that has one |
| `readAloudPlaybackUnavailable` | `Your recording cannot be played back on this page.` | In place of the player for the learner's own take, and of the buttons that play one word of it, when the page refuses to play it — most often a Content-Security-Policy whose `media-src` does not allow `blob:`. Said rather than left as controls that do nothing when pressed |
| `readAloudRecordingProgress(seconds, maxSeconds)` | `Recording: 7 of 20 seconds` | Shown while recording, beside the level meter, and deliberately **not announced**: a counter read out every second would be spoken into the take being assessed. Whole seconds, not a `m:ss` clock: a take is bounded in seconds by the item, and a translation should not have to reimplement a clock to change the word around it |
| `readAloudRecordingStarted` | `Recording started.` | Announced once, when a take starts |
| `readAloudRecordingStopped(seconds, maxSeconds)` | `Recording stopped. 20 of 20 seconds recorded.` | Announced once, when a take ends — whether the learner stopped it or it reached the item's limit. It carries the length because the counter above is not announced, so this is how a screen-reader user hears that a take stopped itself |
| `readAloudTakesRemaining(remaining, max)` | `2 of 5 recordings left` | Called with 0 as well, so one translation covers "none left" too |
| `readAloudSubmitWithoutRecording` | `Submit without recording` | `exam` only: hands the item in with no recording at all, deliberately |
| `readAloudUploading` | `Sending your recording…` | Pending, while the take travels to your storage |
| `readAloudUploadFailed` | `Your recording could not be sent.` | The take never reached storage, so nothing was submitted — a failed upload never becomes a blank answer |
| `readAloudTryAgain` | `Try again` | Retries whichever step just failed |
| `readAloudAssessing` | `Checking your pronunciation…` | Pending, while your assessor judges the take |
| `readAloudAssessmentUnavailable` | `Pronunciation feedback is not available for this activity.` | The binding stores takes but judges none, so there is no feedback to show |
| `readAloudNotHeard` | `We could not hear you. Record again somewhere quieter.` | The assessor found no speech, or too little of it |
| `readAloudNotAssessed` | `This recording could not be assessed. Try recording it again.` | Any other refusal. The code itself is never shown |
| `readAloudAssessmentFailed` | `Your pronunciation could not be checked.` | The assessment could not be run at all — a failure of the call, not of the reading |
| `readAloudRecorderError(reason)` | `This page is not allowed to use the microphone.` | One function, five sentences: `permission-denied`, `no-device`, `unsupported`, `too-short` and `failed`. One key rather than five, so a translation keeps them together |

### Pronunciation feedback

| Key | English default | Where |
|---|---|---|
| `pronunciationFeedbackLabel` | `Pronunciation feedback` | Accessible name of the whole panel |
| `pronunciationDimension(dimension)` | `Accuracy` | Names one scored dimension: `accuracy`, `fluency`, `completeness` or `prosody` |
| `pronunciationNotAssessed` | `Not assessed` | A dimension or a word nobody measured — **never rendered as 0%**, which is a different finding |
| `pronunciationWordsLabel` | `Your reading, word by word` | Accessible name of the marked word list |
| `pronunciationLegend(state)` | `Read correctly` | One row per state: `correct`, `mispronounced`, `omitted`, `inserted` |
| `pronunciationWordCorrect(word)` | `“lovely” was read correctly` | The hidden sentence a screen reader hears for a marked word — the marks' only assistive-technology channel; the glyph and the decoration are hidden from it |
| `pronunciationWordMispronounced(word)` | `“lovely” was mispronounced` | |
| `pronunciationWordOmitted(word)` | `“lovely” was not read` | |
| `pronunciationWordInserted(word)` | `“um” was added` | Called with what was HEARD, not with a reference word — there was none |
| `pronunciationWordDetails(word)` | `Details for “lovely”` | Opens one word's syllables, sounds and timings |
| `pronunciationSyllables` | `Syllables` | |
| `pronunciationPhonemes` | `Sounds` | "Sounds", not "phonemes": the panel is read by learners |
| `pronunciationSyllableSpelling(syllable, grapheme)` | `ˈlʌv (spelled “lov”)` | A syllable the engine also spelled out |
| `pronunciationPhonemePosition(ordinal)` | `Sound 2` | A sound the engine named no symbol for; `ordinal` is 1-based |
| `pronunciationHeardAs` | `Heard as` | Introduces the sounds the engine thought it heard in one sound's place |
| `pronunciationPlayWord(word)` | `Play “lovely”` | Plays one word out of the learner's own take |
| `pronunciationBreakUnexpected` | `There was an unexpected pause here.` | Shown only past the `breakThreshold` you pass; the SDK sets none |
| `pronunciationBreakMissing` | `A pause was expected here.` | |
| `pronunciationMonotone` | `Your reading stayed on one note. Try varying your pitch.` | Shown only past your own `monotoneThreshold` |
| `pronunciationIpaNote` | `Sounds are written in the International Phonetic Alphabet.` | Only when the assessment says its symbols are IPA |

### Authoring preview

| Key | English default | Where |
|---|---|---|
| `previewIncomplete` | `This activity is not finished yet, so it cannot be previewed.` | `<ActivityPreview>` (8.0.0), in place of a draft with something still to write |
| `previewInvalid` | `This activity has a problem to fix before it can be previewed.` | `<ActivityPreview>` (8.0.0), in place of a draft with something wrong in it |

These two address an author rather than a learner. The issues behind them are
not rendered: `validateDraft` writes its messages in English, so a preview that
showed them would put untranslatable text on screen. Pass `fallback` to list
them in your own words: translate the codes the authoring guide documents, and
give any other code one generic message.

### Shared stimulus

| Key | English default | Where |
|---|---|---|
| `stimulusKind.text` | `Passage` | Accessible name of an untitled stimulus region |
| `stimulusKind.audio` | `Recording` | |
| `stimulusKind.video` | `Video` | |
| `stimulusKind.image` | `Image` | |
| `stimulusKind.mixed` | `Material` | |
| `stimulusRange(first, last)` | `Questions 3–8` | Called with equal `first` and `last` for a single question, so a translation picks its own singular |

`stimulusKind` is keyed by kind rather than being one word, because "Recording"
and "Passage" do not share a gender or an article in every language.

### Interactive video

The player's own words. Play, pause, mute, volume, speed, the progress bar's
name and the time readout are the `media` strings under *Audio transport*: the
same controls, the same words. The questions inside a quiz are ordinary
activities and use their own keys above.

| Key | English default | Where |
|---|---|---|
| `videoBack(seconds)` | `Back 10 seconds` | Accessible name of the jump-back button |
| `videoForward(seconds)` | `Forward 10 seconds` | Accessible name of the jump-forward button |
| `videoReplay` | `Replay` | The play button once the video has ended |
| `videoShowRemaining` | `Show time remaining` | The time readout, while it shows the time elapsed: what a press shows next |
| `videoShowElapsed` | `Show time elapsed` | The time readout, while it shows the time remaining |
| `videoCaptionsShow` | `Show captions` | The captions button, while captions are off |
| `videoCaptionsHide` | `Hide captions` | The captions button, while captions are on |
| `videoCaptionsFailed` | `Captions could not be loaded.` | In the captions menu when the caption file could not be fetched or read |
| `videoSecondaryCaptionsFailed` | `The second language could not be loaded.` | In the captions menus when the second language's file could not be fetched or read. The first line keeps working |
| `videoSpeedValue(rate)` | `1.25×` (`Normal` at 1) | Each row of the speed menu |
| `videoSettings` | `Settings` | The settings button and its menu |
| `videoCaptionLanguage` | `Captions` | Settings row and page for the caption track; also the label of a track built from a bare `captionsUrl` |
| `videoSecondCaptionLanguage` | `Second language` | The captions page's item for a second caption line under the first, and the title of the page it opens |
| `videoCaptionPair(primary, secondary)` | `English + Español` | The settings menu's captions row while two languages show, from the tracks' own labels. A translation may order them as its language reads; the first argument is always the upper line |
| `videoNoSecondLanguage` | `No second language for this video.` | Announced when Shift + C finds no other language to show |
| `videoCaptionSize` | `Caption size` | Settings row and page |
| `videoCaptionSizeValue(size)` | `Medium` (`Small`, `Large`) | The three caption sizes |
| `videoCaptionBackground` | `Caption background` | Settings row: a solid box behind the captions |
| `videoKeyboardShortcuts` | `Keyboard shortcuts` | Settings row that turns them on and off, and the title of the shortcut list |
| `videoShortcutList` | `Shortcut list` | Settings row that opens the list |
| `videoClose` | `Close` | Closes the shortcut list |
| `videoOn` | `On` | A setting's state in the settings menu |
| `videoOff` | `Off` | A setting's state, and the no-captions row |
| `videoShortcut(action)` | `Play or pause` | Each row of the shortcut list, by action |
| `videoPanel` | `Contents and transcript` | The button that opens the panel below the video, and the panel's name |
| `videoContents` | `Contents` | The panel's tab of chapters and quizzes |
| `videoTranscript` | `Transcript` | The panel's tab of caption lines |
| `videoTranscriptSearch` | `Search the transcript` | The transcript's search box |
| `videoTranscriptNoMatch` | `No lines match.` | The transcript, when a search matches nothing |
| `videoPictureInPicture` | `Picture in picture` | The picture-in-picture button, where the browser has one |
| `videoFullscreen` | `Fullscreen` | The fullscreen button |
| `videoExitFullscreen` | `Exit fullscreen` | The fullscreen button, in fullscreen |
| `videoError(code)` | `The video could not be loaded.` | Over the video when it fails, by `MediaError.code`: 1 aborted, 2 network, 3 decode, 4 source, 0 anything else |
| `videoTryAgain` | `Try again` | Beside that message |
| `videoQuiz` | `Quiz` | The heading of a quiz with no `title` of its own |
| `videoQuestionProgress(index, total)` | `Question 2 of 5` | A quiz's header, and announced on moving between its questions |
| `videoQuestionStep(index, answered)` | `Question 2, answered` | Accessible name of each dot in a quiz's step indicator |
| `videoNextQuestion` | `Next question` | A quiz's footer, before its last question |
| `videoContinue` | `Continue video` | A quiz's footer, on its last question |
| `videoSkipQuiz` | `Skip quiz` | A quiz's footer, when the quiz is not required |
| `videoRewatch` | `Rewatch` | A quiz's footer: back to the chapter or quiz before it |
| `videoAnswerSaved` | `Answer saved` | A quiz's footer in `exam`, once the question on screen is submitted |
| `videoAnswerPending` | `Saving your answer…` | The end card, beside a Finish that waits while an answer is still being stored or graded |
| `videoRequired` | `Required` | Tag on a required quiz, in its header and in the contents list |
| `videoQuizProgress(answered, total)` | `2 of 3 answered` (`Answered`, `Not started`) | A quiz's row in the contents list and on the end card, and its marker's preview |
| `videoQuizOpened(time, questions)` | `Video paused. Quiz at 0:50, 3 questions.` | Announced when a quiz opens |
| `videoHeldAtQuiz(time)` | `Finish the quiz at 2:00 to continue.` | Announced when a seek stops at a required quiz |
| `videoHeldAhead` | `You can rewind, but not skip ahead.` | Announced when a seek stops at the furthest point reached (`no-skip-ahead`) |
| `videoEnded` | `You reached the end` | Heading of the end card |
| `videoEndScore(answered, total, percent)` | `You answered 7 of 8 · 88%` | End card, `practice` with graded questions |
| `videoEndAnswered(answered, total)` | `You answered 7 of 8 questions` | End card, `exam`, and `practice` with nothing graded |
| `videoWatchAgain` | `Watch again` | End card |
| `videoFinish` | `Finish` | End card, outside `review`: calls `onFinished` |

### Embedded media

| Key | English default | Where |
|---|---|---|
| `embeddedMedia` | `Embedded media` | Accessible name of a provider iframe whose author supplied no `alt`. `MediaSchema` requires `alt` on an embed, so validated content never reaches it — but `validateActivity` is opt-in |

### Error boundary

| Key | English default | Where |
|---|---|---|
| `activityFailed` | `Activity failed to render` | Development only, above the stack |
| `activityFailedNamed(title)` | `"Essay 1" could not be displayed.` | Production, when the activity has a title |
| `activityFailedUnnamed` | `This activity could not be displayed.` | Production, when it does not |

### AI help

| Key | English default | Where |
|---|---|---|
| `aiExplain` | `Explain my answer` | The button under a graded answer, when the host supplies an `explain` port |
| `aiExplaining` | `Explaining…` | The same button while the explanation is written |
| `aiExplanation` | `Explanation` | The heading over an explanation, and its accessible name |
| `aiExplanationUnavailable` | `No explanation is available right now.` | The port failed, or what it returned was refused |
| `aiHint` | `Get a hint` | The button before submit, when the host supplies a `hint` port |
| `aiHintLoading` | `Finding a hint…` | The same button while the hint is written |
| `aiHintNumber(n)` | `Hint 1` | The label before each hint |
| `aiHints` | `Hints` | The accessible name of the list of hints |
| `aiHintUnavailable` | `No hint is available right now.` | The port failed, or its hint gave the answer away |
| `aiNoMoreHints` | `No more hints for this question.` | In place of the hint button once every hint is used |
| `aiNotice` | `Written by AI. It can make mistakes.` | Beneath every explanation, list of hints and feedback on a draft |
| `aiWritingFeedback` | `Get feedback on my draft` | The button before a written response's submit, when the host supplies a `writingFeedback` port |
| `aiWritingFeedbackLoading` | `Reading your draft…` | The same button while the feedback is written |
| `aiWritingFeedbackHeading` | `Feedback on your draft` | The heading over feedback on a draft, and its accessible name |
| `aiWritingFeedbackUnavailable` | `No feedback is available right now.` | The port failed, or its feedback was refused |
| `aiNoMoreWritingFeedback` | `No more feedback for this answer.` | In place of the feedback button once every request is used |
| `aiWritingFeedbackOutdated` | `You have changed your text since this feedback.` | Above feedback on an earlier draft: its corrections may be out of date |
| `aiCorrections` | `Suggested corrections` | The accessible name of the list of corrections in feedback on a draft |
| `aiIndicativeScore(percent)` | `Indicative score: 67%. Not a grade.` | Under feedback that judged every criterion of the rubric |

### Audio transport

Nested under `media`. Identical in shape to the `mediaStrings` prop shipped in
7.0.0, which still works and still wins over the provider.

| Key | English default |
|---|---|
| `media.play` | `Play` |
| `media.pause` | `Pause` |
| `media.preparing` | `Preparing…` |
| `media.mute` | `Mute` |
| `media.unmute` | `Unmute` |
| `media.volume` | `Volume` |
| `media.speed` | `Playback speed` |
| `media.seek` | `Seek` |
| `media.timeValue(elapsed, duration)` | `1:05 of 4:30` — takes already-formatted `m:ss` strings, so a translation need not reimplement the clock |
| `media.playsRemaining(remaining, max)` | `2 of 3 plays remaining` |
| `media.noPlaysRemaining` | `No plays remaining` |
| `media.lastPlayConfirm` | `This is your last play. Start it now?` |
| `media.lastPlayStart` | `Start last play` |
| `media.lastPlayCancel` | `Not yet` |
| `media.seekBlocked` | `Rewinding is not available for this recording.` |
| `media.rateBlocked` | `Playback speed is fixed for this recording.` |
| `media.playFailed` | `The recording could not be started. Try again.` |

## Exports

From `@intellectif/lk-react` (barrel) and `@intellectif/lk-react/i18n/LkIntlProvider`:

| Export | What it is |
|---|---|
| `LkIntlProvider` | The provider: `strings`, `locale`, `direction` |
| `useLkStrings(override?)` | The strings in force, with an optional per-component override on top |
| `useLkDirection()` | `'ltr'` or `'rtl'` |
| `directionForLocale(locale)` | BCP 47 tag → direction |
| `DEFAULT_STRINGS` | The English dictionary — a useful base for a translation |
| `mergeStrings(base, override)` | The two-level merge the provider uses |
| `LkStrings`, `LkStringsOverride`, `LkDirection`, `LkIntlProviderProps` | Types |
